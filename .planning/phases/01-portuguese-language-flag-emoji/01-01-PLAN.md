---
phase: 01-portuguese-language-flag-emoji
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - i18n/routing.ts
  - messages/pt/common.json
  - messages/pt/home.json
  - messages/pt/pricing.json
  - messages/pt/about.json
  - messages/pt/report.json
  - messages/en/common.json
  - messages/fr/common.json
  - messages/es/common.json
  - messages/it/common.json
  - messages/de/common.json
  - components/language-switcher.tsx
autonomous: true
requirements: [I18N-04, I18N-05]

must_haves:
  truths:
    - "Visiting /pt renders Portuguese content on all 4 public pages"
    - "Visiting /pt/pricing renders Portuguese pricing content"
    - "LanguageSwitcher dropdown shows flag emoji next to every language name"
    - "LanguageSwitcher dropdown lists all 6 languages including Portugues"
    - "npm run build succeeds with all 6 locale variants"
  artifacts:
    - path: "i18n/routing.ts"
      provides: "pt locale in routing config"
      contains: "'pt'"
    - path: "messages/pt/common.json"
      provides: "Portuguese common translations"
      contains: "\"nav\""
    - path: "messages/pt/home.json"
      provides: "Portuguese home page translations"
      contains: "\"hero\""
    - path: "messages/pt/pricing.json"
      provides: "Portuguese pricing page translations"
      contains: "\"tiers\""
    - path: "messages/pt/about.json"
      provides: "Portuguese about page translations"
      contains: "\"founder\""
    - path: "messages/pt/report.json"
      provides: "Portuguese report page translations"
      contains: "\"chapters\""
    - path: "components/language-switcher.tsx"
      provides: "Flag emoji in language names, pt entry"
      contains: "Portugu"
  key_links:
    - from: "i18n/routing.ts"
      to: "components/language-switcher.tsx"
      via: "Locale type derived from routing.locales includes pt"
      pattern: "'pt'"
    - from: "messages/pt/*.json"
      to: "i18n/request.ts"
      via: "dynamic import resolves pt locale automatically"
      pattern: "messages/${locale}"
    - from: "components/language-switcher.tsx"
      to: "routing.locales"
      via: "LANGUAGE_NAMES keys must match all locales"
      pattern: "LANGUAGE_NAMES"
---

<objective>
Add Portuguese (pt) as the 6th supported language with fully translated message files, and add flag emoji to all language names in the LanguageSwitcher dropdown.

Purpose: Complete i18n coverage for the Brazilian industrial market (Portuguese) and improve language switcher UX with visual flag indicators.
Output: 5 Portuguese message JSON files, updated routing config, updated LanguageSwitcher with flags for all 6 languages.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-portuguese-language-flag-emoji/01-RESEARCH.md

@i18n/routing.ts
@i18n/request.ts
@components/language-switcher.tsx
@messages/en/common.json
@messages/en/home.json
@messages/en/pricing.json
@messages/en/about.json
@messages/en/report.json

<interfaces>
<!-- Locale type is auto-derived from routing.locales array -->
From i18n/routing.ts:
```typescript
export const routing = defineRouting({
  locales: ['en', 'fr', 'es', 'it', 'de'],
  defaultLocale: 'en',
  localePrefix: 'as-needed',
  localeCookie: { name: 'NEXT_LOCALE', maxAge: 31536000 },
})
export type Locale = (typeof routing.locales)[number]
```

From i18n/request.ts (no changes needed -- dynamic import auto-resolves):
```typescript
messages: {
  Common: (await import(`../messages/${locale}/common.json`)).default,
  Home: (await import(`../messages/${locale}/home.json`)).default,
  Pricing: (await import(`../messages/${locale}/pricing.json`)).default,
  About: (await import(`../messages/${locale}/about.json`)).default,
  Report: (await import(`../messages/${locale}/report.json`)).default,
}
```

From components/language-switcher.tsx:
```typescript
const LANGUAGE_NAMES: Record<Locale, string> = {
  en: "English",
  fr: "Francais",
  es: "Espanol",
  it: "Italiano",
  de: "Deutsch",
}
```

Current common.json language section pattern (same in all 5 locales):
```json
"language": {
  "en": "English",
  "fr": "Francais",
  "es": "Espanol",
  "it": "Italiano",
  "de": "Deutsch",
  "switchLanguage": "Switch language"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Portuguese locale to routing and create 5 translated message files</name>
  <files>i18n/routing.ts, messages/pt/common.json, messages/pt/home.json, messages/pt/pricing.json, messages/pt/about.json, messages/pt/report.json</files>
  <read_first>i18n/routing.ts, messages/en/common.json, messages/en/home.json, messages/en/pricing.json, messages/en/about.json, messages/en/report.json</read_first>
  <action>
1. Update i18n/routing.ts: Add 'pt' to the locales array so it reads:
   locales: ['en', 'fr', 'es', 'it', 'de', 'pt']
   No other changes needed in this file. The Locale type auto-derives from this array.

2. Create messages/pt/ directory and 5 JSON files with Portuguese (Brazilian) translations.

   For translations: Try Ollama qwen2.5 at http://localhost:11434 first with a curl request. If Ollama is unavailable, write translations manually using Claude's Portuguese knowledge. These are draft translations -- professional review recommended per PROJECT.md.

   CRITICAL RULES for translations:
   - Copy the EXACT key structure from each English file -- translate ONLY the string values
   - NEVER translate brand names: ThreadMoat, Digital Thread, Recon, The Forge, The Red Keep, 3DEXPERIENCE, Kernel Wars, Threaded!
   - Preserve interpolation variables like {year}, {lastUpdated} exactly as-is
   - Use Brazilian Portuguese conventions (not European Portuguese)
   - Keep technical terms like "PLM", "CAD", "CAE", "SaaS", "B2B" untranslated

   For messages/pt/common.json, the language section should list all 6 languages (without emoji -- emoji go in the component constant only):
   ```json
   "language": {
     "en": "English",
     "fr": "Francais",
     "es": "Espanol",
     "it": "Italiano",
     "de": "Deutsch",
     "pt": "Portugues",
     "switchLanguage": "Mudar idioma"
   }
   ```
  </action>
  <verify>
    <automated>cd /Users/mfinocchiaro/Dropbox/Private/Obsidian/Projects/threadmoat-website && ls messages/pt/common.json messages/pt/home.json messages/pt/pricing.json messages/pt/about.json messages/pt/report.json && grep "'pt'" i18n/routing.ts && node -e "const en=Object.keys(require('./messages/en/home.json'));const pt=Object.keys(require('./messages/pt/home.json'));console.log('Key match:',JSON.stringify(en)===JSON.stringify(pt))"</automated>
  </verify>
  <acceptance_criteria>
    - i18n/routing.ts contains 'pt' in locales array: grep "'pt'" i18n/routing.ts
    - All 5 Portuguese message files exist in messages/pt/
    - messages/pt/common.json contains "nav" key with Portuguese values
    - messages/pt/home.json has same top-level keys as messages/en/home.json (meta, hero, thesis, profiles, services, expertise, cta)
    - messages/pt/pricing.json has same top-level keys as messages/en/pricing.json (meta, hero, tiers, sample, outputs, methodology, features, chapters)
    - messages/pt/about.json has same top-level keys as messages/en/about.json (meta, about, founder, conference, podcasts, contact)
    - messages/pt/report.json has same top-level keys as messages/en/report.json (meta, hero, stats, toc, audience, methodology, cta, chapters)
    - Brand names "ThreadMoat", "Recon", "The Forge", "The Red Keep" appear untranslated in Portuguese files
    - Interpolation variables {year} and {lastUpdated} preserved in Portuguese files
    - messages/pt/common.json language section includes "pt": "Portugues"
  </acceptance_criteria>
  <done>All 5 Portuguese message files exist with correct key structures matching English, routing.ts includes pt in locales array, brand names preserved verbatim</done>
</task>

<task type="auto">
  <name>Task 2: Add flag emoji to LanguageSwitcher and update all common.json language sections</name>
  <files>components/language-switcher.tsx, messages/en/common.json, messages/fr/common.json, messages/es/common.json, messages/it/common.json, messages/de/common.json, messages/pt/common.json</files>
  <read_first>components/language-switcher.tsx, messages/en/common.json, messages/fr/common.json, messages/es/common.json, messages/it/common.json, messages/de/common.json, messages/pt/common.json</read_first>
  <action>
1. Update components/language-switcher.tsx:
   a. Replace the LANGUAGE_NAMES constant with flag emoji per user specification (LOCKED decisions):
      ```typescript
      const LANGUAGE_NAMES: Record<Locale, string> = {
        en: "\u{1F1FA}\u{1F1F8}\u{1F1EC}\u{1F1E7} English",
        fr: "\u{1F1EB}\u{1F1F7} Fran\u00e7ais",
        es: "\u{1F1EA}\u{1F1F8} Espa\u00f1ol",
        it: "\u{1F1EE}\u{1F1F9} Italiano",
        de: "\u{1F1E9}\u{1F1EA} Deutsch",
        pt: "\u{1F1E7}\u{1F1F7}\u{1F1F5}\u{1F1F9} Portugu\u00eas",
      }
      ```
      Use the actual emoji characters in source code, not escape sequences. The above shows the mapping:
      - en: US+UK flags (two flag emoji side by side)
      - fr: French flag
      - es: Spanish flag
      - it: Italian flag
      - de: German flag
      - pt: Brazil+Portugal flags (two flag emoji side by side)

   b. Widen the dropdown from w-36 to w-44 to accommodate dual-flag entries without truncation:
      Change: className="w-36" to className="w-44"
      on the DropdownMenuContent element.

2. Update the "language" section in ALL 6 common.json files to add the "pt" entry.
   Each locale's common.json language section should become:
   ```json
   "language": {
     "en": "English",
     "fr": "Francais",
     "es": "Espanol",
     "it": "Italiano",
     "de": "Deutsch",
     "pt": "Portugues",
     "switchLanguage": "[existing translated value]"
   }
   ```
   The switchLanguage value stays as-is in each locale (already translated). Only ADD the "pt" line.

   Specific switchLanguage values to preserve:
   - en: "Switch language"
   - fr: "Changer de langue"
   - es: (read from file)
   - it: (read from file)
   - de: (read from file)
   - pt: "Mudar idioma" (already set in Task 1)

   NOTE: Do NOT add emoji to common.json values. The LANGUAGE_NAMES constant in the component is the display source. The common.json language section is a secondary reference that stays plain text.
  </action>
  <verify>
    <automated>cd /Users/mfinocchiaro/Dropbox/Private/Obsidian/Projects/threadmoat-website && grep -c "pt:" components/language-switcher.tsx && grep "w-44" components/language-switcher.tsx && for lang in en fr es it de pt; do echo "$lang:"; node -e "const c=require('./messages/$lang/common.json');console.log('has pt:','pt' in c.language)"; done && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - components/language-switcher.tsx LANGUAGE_NAMES has 6 entries including pt
    - components/language-switcher.tsx contains flag emoji characters (grep for "English" should show flag before it)
    - DropdownMenuContent className contains "w-44" (widened from w-36)
    - All 6 common.json files contain "pt" key in their language section: grep '"pt"' messages/*/common.json returns 6 matches
    - npm run build succeeds without errors
  </acceptance_criteria>
  <done>LanguageSwitcher shows flag emoji next to all 6 language names, dropdown width accommodates dual flags, all common.json language sections include pt entry, build passes</done>
</task>

</tasks>

<verification>
1. `npm run build` completes successfully (validates all locale message imports resolve)
2. All 6 message directories exist with 5 files each: `ls messages/*/`
3. Portuguese routing works: `grep "'pt'" i18n/routing.ts`
4. Flags in switcher: `grep -P '[\x{1F1E6}-\x{1F1FF}]' components/language-switcher.tsx` (or visual grep for flag emoji)
5. All common.json files have pt in language section: `grep '"pt"' messages/*/common.json`
</verification>

<success_criteria>
- npm run build succeeds with 6 locales (en, fr, es, it, de, pt)
- Visiting /pt/pricing would render Portuguese pricing content
- LanguageSwitcher dropdown shows flag emoji next to each language name
- All 6 locale message files have matching key structures
- Brand names (ThreadMoat, Recon, The Forge, The Red Keep) remain untranslated in Portuguese
</success_criteria>

<output>
After completion, create `.planning/phases/01-portuguese-language-flag-emoji/01-01-SUMMARY.md`
</output>
