---
phase: 14-internationalization-i18n-for-public-website-pages
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - components/language-switcher.tsx
  - app/[locale]/page.tsx
  - app/[locale]/pricing/page.tsx
  - app/[locale]/about/page.tsx
  - app/[locale]/report/page.tsx
  - messages/fr/common.json
  - messages/fr/home.json
  - messages/fr/pricing.json
  - messages/fr/about.json
  - messages/fr/report.json
  - messages/es/common.json
  - messages/es/home.json
  - messages/es/pricing.json
  - messages/es/about.json
  - messages/es/report.json
  - messages/it/common.json
  - messages/it/home.json
  - messages/it/pricing.json
  - messages/it/about.json
  - messages/it/report.json
  - messages/de/common.json
  - messages/de/home.json
  - messages/de/pricing.json
  - messages/de/about.json
  - messages/de/report.json
autonomous: false
requirements: []

must_haves:
  truths:
    - "Globe icon dropdown appears in header next to ThemeToggle on all 4 pages"
    - "Clicking a language in the dropdown navigates to the locale-prefixed URL"
    - "Language names display in native script (Francais, Espanol, Italiano, Deutsch)"
    - "French, Spanish, Italian, and German message files exist with translated content"
    - "Visiting /fr/pricing renders French pricing page content"
    - "npm run build succeeds with all locale variants"
  artifacts:
    - path: "components/language-switcher.tsx"
      provides: "Globe dropdown client component with 5 language options"
      contains: "LanguageSwitcher"
    - path: "messages/fr/common.json"
      provides: "French shared translations"
    - path: "messages/es/common.json"
      provides: "Spanish shared translations"
    - path: "messages/it/common.json"
      provides: "Italian shared translations"
    - path: "messages/de/common.json"
      provides: "German shared translations"
  key_links:
    - from: "components/language-switcher.tsx"
      to: "i18n/navigation.ts"
      via: "import useRouter, usePathname from @/i18n/navigation"
      pattern: "import.*useRouter.*from.*@/i18n/navigation"
    - from: "app/[locale]/page.tsx"
      to: "components/language-switcher.tsx"
      via: "LanguageSwitcher rendered in header area"
      pattern: "LanguageSwitcher"
---

<objective>
Create the language switcher component, add it to all 4 page headers, and generate draft translations for French, Spanish, Italian, and German.

Purpose: Completes the user-facing i18n experience — visitors can switch languages via the globe dropdown, and all 4 non-English locales have translated content. This is the final functional plan before verification.

Output: LanguageSwitcher component in all headers, 20 translated JSON files (4 languages x 5 files each), working language switching.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-internationalization-i18n-for-public-website-pages/14-RESEARCH.md
@.planning/phases/14-internationalization-i18n-for-public-website-pages/14-01-SUMMARY.md
@.planning/phases/14-internationalization-i18n-for-public-website-pages/14-02-SUMMARY.md

@i18n/navigation.ts
@components/theme-toggle.tsx
@messages/en/common.json
@messages/en/home.json
@messages/en/pricing.json
@messages/en/about.json
@messages/en/report.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LanguageSwitcher component and add to all page headers</name>
  <files>components/language-switcher.tsx, app/[locale]/page.tsx, app/[locale]/pricing/page.tsx, app/[locale]/about/page.tsx, app/[locale]/report/page.tsx</files>
  <read_first>
    - components/theme-toggle.tsx (reference for styling — switcher sits next to this)
    - app/[locale]/page.tsx (header section where switcher will be added)
    - app/[locale]/pricing/page.tsx (header section)
    - app/[locale]/about/page.tsx (header section)
    - app/[locale]/report/page.tsx (header section)
    - i18n/navigation.ts (exports useRouter, usePathname for locale switching)
    - .planning/phases/14-internationalization-i18n-for-public-website-pages/14-RESEARCH.md (Pattern 6: Language Switcher)
  </read_first>
  <action>
1. Create `components/language-switcher.tsx`:
```typescript
'use client'

import { useRouter, usePathname } from '@/i18n/navigation'
import { Globe } from 'lucide-react'
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuTrigger,
} from '@/components/ui/dropdown-menu'
import { Button } from '@/components/ui/button'

const LOCALES = [
  { code: 'en', label: 'English' },
  { code: 'fr', label: 'Francais' },
  { code: 'es', label: 'Espanol' },
  { code: 'it', label: 'Italiano' },
  { code: 'de', label: 'Deutsch' },
] as const

export function LanguageSwitcher({ currentLocale }: { currentLocale: string }) {
  const router = useRouter()
  const pathname = usePathname()

  return (
    <DropdownMenu>
      <DropdownMenuTrigger asChild>
        <Button variant="ghost" size="icon" className="size-8" aria-label="Switch language">
          <Globe className="h-4 w-4" />
        </Button>
      </DropdownMenuTrigger>
      <DropdownMenuContent align="end">
        {LOCALES.map((loc) => (
          <DropdownMenuItem
            key={loc.code}
            onClick={() => router.replace(pathname, { locale: loc.code })}
            className={currentLocale === loc.code ? 'font-semibold' : ''}
          >
            {loc.label}
          </DropdownMenuItem>
        ))}
      </DropdownMenuContent>
    </DropdownMenu>
  )
}
```

IMPORTANT: `useRouter` and `usePathname` MUST be imported from `@/i18n/navigation`, NOT from `next/navigation`. This is critical for locale-aware routing and cookie management.

2. Add `<LanguageSwitcher>` to the header of ALL 4 pages, placed directly before or after the `<ThemeToggle />` component in the header's button group area.

In each page's header section, find the `<ThemeToggle />` and add the LanguageSwitcher next to it:
```tsx
import { LanguageSwitcher } from '@/components/language-switcher'

// In the header's button group:
<div className="flex items-center gap-3">
  <LanguageSwitcher currentLocale={locale} />
  <ThemeToggle />
  {/* ... Sign In, Schedule Call buttons ... */}
</div>
```

The `locale` variable is already available in each page from `const { locale } = await params`.

Since LanguageSwitcher is a Client Component used inside Server Component pages, pass `currentLocale` as a prop (string serializable).

3. Verify all 4 pages have the LanguageSwitcher import and usage.
  </action>
  <verify>
    <automated>cd /Users/mfinocchiaro/Dropbox/Private/Obsidian/Projects/threadmoat-website && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `components/language-switcher.tsx` exists and contains `'use client'` directive
    - `components/language-switcher.tsx` imports `useRouter` from `@/i18n/navigation` (NOT `next/navigation`)
    - `components/language-switcher.tsx` imports `usePathname` from `@/i18n/navigation`
    - `components/language-switcher.tsx` contains `Globe` import from `lucide-react`
    - `components/language-switcher.tsx` contains `DropdownMenu` import from `@/components/ui/dropdown-menu`
    - `components/language-switcher.tsx` contains all 5 locale labels: `English`, `Francais`, `Espanol`, `Italiano`, `Deutsch`
    - `components/language-switcher.tsx` exports `LanguageSwitcher` function component
    - `app/[locale]/page.tsx` contains `import { LanguageSwitcher }` and `<LanguageSwitcher`
    - `app/[locale]/pricing/page.tsx` contains `import { LanguageSwitcher }` and `<LanguageSwitcher`
    - `app/[locale]/about/page.tsx` contains `import { LanguageSwitcher }` and `<LanguageSwitcher`
    - `app/[locale]/report/page.tsx` contains `import { LanguageSwitcher }` and `<LanguageSwitcher`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>LanguageSwitcher component created with globe icon dropdown, added to all 4 page headers next to ThemeToggle, uses locale-aware useRouter from i18n/navigation.</done>
</task>

<task type="auto">
  <name>Task 2: Generate draft translations for French, Spanish, Italian, and German</name>
  <files>messages/fr/*.json, messages/es/*.json, messages/it/*.json, messages/de/*.json</files>
  <read_first>
    - messages/en/common.json (source strings)
    - messages/en/home.json (source strings)
    - messages/en/pricing.json (source strings)
    - messages/en/about.json (source strings)
    - messages/en/report.json (source strings)
  </read_first>
  <action>
Generate translations for all 4 languages (fr, es, it, de) using Ollama qwen2.5 locally.

Create directories: `mkdir -p messages/fr messages/es messages/it messages/de`

For each of the 5 English JSON files (common.json, home.json, pricing.json, about.json, report.json), translate into each target language:

**Translation workflow per file per language:**
```bash
# Example for French home.json
curl -s http://localhost:11434/api/generate \
  -d '{
    "model": "qwen2.5",
    "prompt": "Translate this JSON from English to French. Return ONLY valid JSON with the same structure and keys. Do NOT translate JSON key names. Do NOT translate brand names: ThreadMoat, Digital Thread, Recon, Forge, Red Keep, Threaded!. Context: B2B SaaS website for industrial AI and engineering software market intelligence.\n\n<json_content_here>",
    "stream": false
  }' | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['response'])"
```

Repeat for each language:
- French (fr): "Translate this JSON from English to French"
- Spanish (es): "Translate this JSON from English to Spanish"
- Italian (it): "Translate this JSON from English to Italian"
- German (de): "Translate this JSON from English to German"

**That's 5 files x 4 languages = 20 translation calls.**

If Ollama is unavailable (connection refused), fall back to writing the translations manually using Claude's knowledge. This is acceptable as a draft — user will review all translations before shipping.

**After generating each JSON file:**
1. Validate it's valid JSON: `python3 -c "import json; json.load(open('messages/fr/home.json'))"`
2. Verify it has the same keys as the English source (same structure)
3. Verify brand names are NOT translated (ThreadMoat, Digital Thread, etc. remain English)

**If Ollama returns malformed JSON:**
- Try parsing and fixing minor issues (trailing commas, unescaped quotes)
- If unfixable, write the translation manually

Write each translated file to `messages/{locale}/{page}.json`.

IMPORTANT: The JSON structure (key names, nesting depth) MUST exactly match the English source files. Only the string VALUES change.
  </action>
  <verify>
    <automated>cd /Users/mfinocchiaro/Dropbox/Private/Obsidian/Projects/threadmoat-website && for lang in fr es it de; do for page in common home pricing about report; do echo -n "$lang/$page.json: "; python3 -c "import json; d=json.load(open('messages/$lang/$page.json')); print('VALID', len(str(d)))" 2>&1; done; done</automated>
  </verify>
  <acceptance_criteria>
    - `messages/fr/common.json`, `messages/fr/home.json`, `messages/fr/pricing.json`, `messages/fr/about.json`, `messages/fr/report.json` all exist and are valid JSON
    - `messages/es/common.json`, `messages/es/home.json`, `messages/es/pricing.json`, `messages/es/about.json`, `messages/es/report.json` all exist and are valid JSON
    - `messages/it/common.json`, `messages/it/home.json`, `messages/it/pricing.json`, `messages/it/about.json`, `messages/it/report.json` all exist and are valid JSON
    - `messages/de/common.json`, `messages/de/home.json`, `messages/de/pricing.json`, `messages/de/about.json`, `messages/de/report.json` all exist and are valid JSON
    - All 20 JSON files pass `python3 -c "import json; json.load(open(f))"` validation
    - French `common.json` contains French text (not English) for nav items
    - Brand name "ThreadMoat" appears un-translated in all locale files where it appears in English
    - All locale files have the same top-level keys as their English counterparts
  </acceptance_criteria>
  <done>All 20 translated JSON files created (4 languages x 5 files), all valid JSON, same structure as English source, brand names preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify language switching and translated content</name>
  <files>none (verification only)</files>
  <action>
Start the dev server with `npm run dev` and present the verification checklist to the user. Run `npm run build` to confirm the build succeeds with all locale variants. No code changes in this task — purely verification and user sign-off.
  </action>
  <verify>
    <automated>cd /Users/mfinocchiaro/Dropbox/Private/Obsidian/Projects/threadmoat-website && npm run build 2>&1 | tail -20</automated>
  </verify>
  <what-built>
Complete i18n system: 4 public pages (home, pricing, about, report) translated into 5 languages (en, fr, es, it, de), language switcher globe dropdown in header, locale-prefixed URLs, Accept-Language auto-detection, NEXT_LOCALE cookie persistence.
  </what-built>
  <how-to-verify>
1. Start dev server: `npm run dev`

2. Visit http://localhost:3000 — should show English home page, no redirect

3. Click the globe icon in the header — dropdown should show:
   - English (current, bold)
   - Francais
   - Espanol
   - Italiano
   - Deutsch

4. Click "Francais" — should navigate to http://localhost:3000/fr with French content

5. Visit http://localhost:3000/fr/pricing — should show French pricing page

6. Visit http://localhost:3000/es/about — should show Spanish about page

7. Visit http://localhost:3000/de/report — should show German report page

8. Hard refresh on /fr/ — should stay French (NEXT_LOCALE cookie persists)

9. Visit http://localhost:3000/dashboard — should NOT redirect to /en/dashboard (no locale routing on dashboard)

10. Run `npm run build` — should succeed without errors

11. Spot-check translation quality on the French pricing page — are brand names (ThreadMoat, Digital Thread) left in English? Are feature descriptions in French?
  </how-to-verify>
  <resume-signal>Type "approved" if language switching and translations work correctly, or describe any issues found.</resume-signal>
  <done>User has verified: language switcher works, translations render correctly, dashboard routes unaffected, npm run build succeeds.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with all locale variants generated
- All 20 translated JSON files are valid JSON
- LanguageSwitcher component renders globe icon in all 4 page headers
- Language switching navigates to correct locale-prefixed URL
- Dashboard routes are not affected by locale routing
- NEXT_LOCALE cookie is set after language switch
</verification>

<success_criteria>
Complete working i18n: 4 pages x 5 languages = 20 locale combinations all rendering correctly. Language switcher globe dropdown in all headers. Accept-Language auto-detection on first visit. Cookie persistence for returning visitors. npm run build succeeds. Dashboard and auth routes unaffected.
</success_criteria>

<output>
After completion, create `.planning/phases/14-internationalization-i18n-for-public-website-pages/14-03-SUMMARY.md`
</output>
